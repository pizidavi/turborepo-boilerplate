{
  "name": "api",
  "version": "0.0.0",
  "scripts": {
    "dev": "mkdir -p public && wrangler dev -e development",
    "deploy": "wrangler deploy --minify -e preview",
    "clean": "rm -r public"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@repo/logger": "workspace:*",
    "@repo/types": "workspace:*",
    "@scalar/hono-api-reference": "^0.5.173",
    "hono": "^4.7.1",
    "hono-openapi": "^0.4.4",
    "zod": "^3.24.2",
    "zod-openapi": "^4.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "openapi-types": "^12.1.3",
    "wrangler": "^3.109.1"
  }
}
